# Rules

本目录用于存放大模型相关的规则配置。

## 目录结构

- `coding/` - 编码规则，包含代码生成、代码审查等规则
- `prompts/` - 提示词规则，包含提示词模板、提示词优化等规则
- `agents/` - Agent规则，包含Agent行为、Agent协作等规则

## 规则格式

每个规则文件应包含以下内容：
- 规则名称
- 规则描述
- 规则内容
- 使用示例